name: <%= projectName %>
recipe: wordpress
proxy:
  appserver: <%= projectName %>.lndo.site
  phpmyadmin: <%= projectName %>-pma.lndo.site
  mailhog: <%= projectName %>-mailhog.lndo.site

config:
  xdebug: true
  php: "<%= phpVersion %>"

services:
  phpmyadmin:
    type: phpmyadmin
    hosts:
      - database

  appserver:
    config:
      php: .lando/php.ini
    build:
      - cd /app/wp-content/themes/<%= themeName %> && composer install
    overrides:
      environment:
        PHP_IDE_CONFIG: "serverName=PHPSTORM"

  mailhog:
    type: mailhog
    portforward: false
    hogfrom:
      - appserver